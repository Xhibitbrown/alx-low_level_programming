#include <stdio.h>

/**
* main - a program that prints the alphabet in lowercase
* Description: prints the alphabet in lowercase
* Return: Always 0
*/

int main(void)
{
	char n;

	for (n = 0; n <= 10;  n++)
	{
		putchar(n);
	}
	putchar('\n');

	return (0);
}
