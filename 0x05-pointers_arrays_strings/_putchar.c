#include "main.h"
#include <unistd.h>
/**
* _strlen - a function that returns the length of a string
* Return: Alway 0
* Description: Printing length of a string

int _strlen(char *s)
{
	return (0);
}
